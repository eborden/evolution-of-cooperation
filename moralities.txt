

function moralities () {
    return [
        function titForTat (opponentHistory, myHistory) {
            if (_.last(opponentHistory) === DEFECT) {
                return DEFECT;
            } else {
                return COOPERATE;
            }
        },
        function llama (opponentHistory, myHistory) {
            return COOPERATE;
        },
        function demon (opponentHistory, myHistory) {
            return DEFECT;
        },
        function () {
            var i = true,
                gen = function () {
                if (i) {
                    return COOPERATE;
                } else {
                    return DEFECT;
                }
                i = !i;
            };
            return function flipFlopNice (opponentHistory, myHistory) {
                return gen();
            };
        }(),
        function () {
            var i = true,
                gen = function () {
                if (i) {
                    return DEFECT;
                } else {
                    return COOPERATE;
                }
                i = !i;
            };
            return function flipFlopNice (opponentHistory, myHistory) {
                return gen();
            };
        }(),
        function random (opponentHistory, myHistory) {
            return Math.round(Math.random()) ? DEFECT : COOPERATE;
        },
        function niceRandom (opponentHistory, myHistory) {
            return Math.round(1 -  Math.random() * .7) ? COOPERATE : DEFECT;
        },
        function evilRandom (opponentHistory, myHistory) {
            return Math.round(Math.random() * .7) ? COOPERATE : DEFECT;
        }
    ];
}